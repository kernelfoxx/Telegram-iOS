# CLAUDE.md

This file provides guidance to AI assistants when working with code in this repository.

## Build

The app is built using Bazel via the `Make.py` wrapper. There is no selective per-module build — the only supported invocation builds the full `Telegram/Telegram` target.

**Command:**

```sh
python3 build-system/Make/Make.py --overrideXcodeVersion \
 --cacheDir ~/telegram-bazel-cache \
 build \
 --configurationPath build-system/appstore-configuration.json \
 --gitCodesigningRepository git@gitlab.com:peter-iakovlev/fastlanematch.git \
 --gitCodesigningType development --gitCodesigningUseCurrent --buildNumber=1 --configuration=debug_sim_arm64
```

Add `--continueOnError` after `build` (forwards to bazel's `--keep_going`) when verifying changes that may surface errors in many files at once — it lets the full set of errors land in one pass instead of stopping at the first failing target.

The build needs `TELEGRAM_CODESIGNING_GIT_PASSWORD` in the environment. It is set in `~/.zshrc` but Claude Code's bash tool does NOT source shell config by default. Prefix build commands with `source ~/.zshrc 2>/dev/null;` to pick it up.

**Running tests.** `Make.py test` runs Bazel test targets (same config + codesigning as `build`, forced `debug_sim_arm64`). It accepts `--target <label>` (added 2026-06-19; default `Tests/AllTests`) so a single `ios_unit_test` can run in isolation, e.g.:

```sh
source ~/.zshrc 2>/dev/null; python3 build-system/Make/Make.py --overrideXcodeVersion --cacheDir ~/telegram-bazel-cache \
 test --configurationPath build-system/appstore-configuration.json \
 --gitCodesigningRepository git@gitlab.com:peter-iakovlev/fastlanematch.git \
 --gitCodesigningType development --gitCodesigningUseCurrent --target //submodules/TextFormat:TextFormatTests
```

The first app-side `ios_unit_test` is `//submodules/TextFormat:TextFormatTests` (the mention/date link codecs). An `ios_unit_test` here needs an `ios_test_runner` pinned to a real device/OS (e.g. `iPhone 17` / `26.5`) — the default runner picks an invalid device and the test process exits 15. **Run new targets via `--target`, not the default suite:** `Tests/AllTests` currently references a dangling `//submodules/TgVoipWebrtc:TgCallsTests`, so the default would fail to build until that suite is repaired.

## Code Style Guidelines
- **Naming**: PascalCase for types, camelCase for variables/methods
- **Imports**: Group and sort imports at the top of files
- **Error Handling**: Properly handle errors with appropriate redaction of sensitive data
- **Formatting**: Use standard Swift/Objective-C formatting and spacing
- **Types**: Prefer strong typing and explicit type annotations where needed
- **Documentation**: Document public APIs with comments

## Project Structure
- Core launch and application extensions code is in `Telegram/` directory
- Most code is organized into libraries in `submodules/`
- External code is located in `third-party/`
- App-side unit tests are minimal: the first `ios_unit_test` (`//submodules/TextFormat:TextFormatTests`) was added 2026-06-19 (run via `Make.py test --target` — see Build). The RichTextEditor SwiftPM package keeps its own suite (`swift test` / `Scripts/iostest.sh`). Most modules still have no tests.

## RichTextEditor editor (`submodules/TelegramUI/Components/RichTextEditor`)

A from-scratch WYSIWYG rich-text editor (UIKit; TextKit 2 on iOS 16+, TextKit 1 on iOS 13–15) developed as a **SwiftPM package** that lives in-tree and is now **wired into the app** — `ChatTextInputPanelNode` depends on `:RichTextEditorUIKit`, so it builds under Bazel at the repo's iOS-13 floor as part of the app. It was historically developed/tested via **SwiftPM** from that directory (`swift test` for Core on macOS, `Scripts/iostest.sh` for the UIKit suite, the `Demo/` xcodegen app); the UIKit target uses a small `#if SWIFT_PACKAGE` switch for its one bundled resource so it still compiles under both build systems (see below). Its own `CLAUDE.md` there is the authority for architecture, status, and workflow.

Both Bazel targets — `//submodules/TelegramUI/Components/RichTextEditor:RichTextEditorCore` and `:RichTextEditorUIKit` — now **fully compile and run down to iOS 13**. TextKit is quarantined behind a swappable seam (`protocol BlockLayoutEngine`): `BlockLayout` (TextKit 2, `@available(iOS 16)`) on iOS 16+, `BlockLayoutTK1` (TextKit 1, iOS 7+) on iOS 13–15, chosen by `makeBlockLayout(...)` / the `BlockLayoutBackend.forceTextKit1` debug toggle (DEBUG default `true`). The system edit menu falls back from `UIEditMenuInteraction` (iOS 16+) to `UIMenuController` (iOS 13–15). So the UIKit target is gated `@available(iOS 13.0, *)`, with only genuine higher-OS APIs kept at their floor: the TK2 `BlockLayout` type + the `UIEditMenuInteraction` touch-points at iOS 16; the loupe (`UITextLoupeSession`) + inline predictions at 17; Translate at 17.4; `isEditable` at 18. TK1 trade-offs on iOS 13–15 (deliberate): no spoiler text-hiding, loupe, or inline predictions — everything else works. Core is pure-Foundation, always-available; the app-side consumers + `StandaloneInstantPageImageView` are lowered to iOS 13 too (editor still gated behind the `debugRichText` flag). The spoiler-texture resource is resolved per build system behind `#if SWIFT_PACKAGE`: SwiftPM uses `.module`, Bazel uses the app's `AppBundle` (`UIImage(bundleImageName: "Components/TextSpeckle")`, `//submodules/AppBundle` dep) — so both build systems compile.

**In-progress feature work on branch `feature/richtext-message-serialization` (as of 2026-06-12).** The editor is now **parent-driven**: `RichTextEditorView.update(size:insets:) -> CGFloat` (parent supplies scroll insets; returns measured height), a payload-free `onChange`, `currentState() -> EditorState` (format/style/list/link/selection/in-table + canUndo/canRedo), and `deleteTable()`; the view no longer observes the keyboard. The host `RichTextAttachmentScreen` (a separate module) drives layout from `onChange` and has a bottom **action bar** (`RichTextActionBarComponent`) whose 12 actions + context menus (ContextUI) are wired from `currentState()`. Custom-emoji layout (baseline-aligned square box + 4pt body boost) and a top edge effect also landed. **Runtime verification (logged-in sim) is still pending**, and a few items are deferred (pending caret-formatting; table structural selection → proper delete-table). Full session handoff + the two spec/plan pairs: `~/Documents/RichTextEditor/docs/superpowers/{handoffs/2026-06-12-richtext-session-handoff.md, specs, plans}`.

**Chat composer text context menu (added 2026-06-17, runtime-verified on iOS 26 sim).** The new editor backend now surfaces the composer's **Format** submenu (Bold/Italic/Monospace/Link/Strikethrough/Underline/Quote/Spoiler/Date/Code, secret-chat gated) in its iOS-16 edit menu, via a new `RichTextEditorView.contextMenuItemsProvider` hook that `ChatTextInputPanelNode` installs (it transforms the editor's default menu elements — drops the editor's built-in 3-item Format submenu, splices in the richer one, preserves Look Up/Translate/Share). Actions route to the editor's **native engine** (`toggle*`/`setParagraphStyle(.quote)`); **Link** is wired through the host `openLinkEditing` (branched in `ChatControllerLoadDisplayNode` for the native backend, using the editor's `currentLink()`/`selectedText()`/`setLink`/`removeLink`); **Date is a deferred no-op; Code now creates a first-class code block** (see the "Composer code blocks" note below — the editor lacks only a timestamp-creation UI for Date). The detailed seam (`ChatRichTextInputNode.contextMenuItemsProvider` / `performFormatAction` / `currentRichTextLinkURL` / `selectedRichText` / `applyRichTextLink` / `usesNativeRichTextEngine`) and the **load-bearing `Display.Window1.hitTest` "EditMenu" match** (without which the iOS-16 edit menu's taps fall through to content app-wide) are documented in the editor's own `CLAUDE.md`. iOS 13–15 keeps the editor's built-in menu (UIMenuController can't carry closure-backed items).

**Composer bridge round-trip — custom emoji / mention / date (added 2026-06-19, `feature/richtext-composer-bridge-gaps`).** The native-engine composer converts to/from the chat `NSAttributedString` currency via `ComposerDocumentBridge` (live) and `EntityMessageBuilder` (send). These now **losslessly round-trip** three previously-dropped inline features: **custom emoji** (`ChatTextInputAttributes.customEmoji` ↔ `EmojiRef`, one `U+FFFC`, original placeholder kept as `altText`), **text mentions**, and **dates** — the latter two encoded into the Document's shared `link` field via `tg://user?id=` / `tg://timestamp?t=` markers from the shared `TextFormat` codec (`MentionDateMarkers.swift`: `mentionMarkdownURL`/`dateMarkdownURL`/`classifyChatLink`/`chatInputLinkAttribute`, with unit tests in `//submodules/TextFormat:TextFormatTests`). The send path is automatic: the builder stamps the chat attributes and `generateChatInputTextEntities` already emits the matching entities. **Creation is unchanged** — the Format▸Date menu item is still a no-op; this is preservation only. (Code-block round-tripping **and** creation landed in the follow-up below — only Date creation remains deferred.) **Known accepted limitation:** a `textUrl` link whose string equals a `tg://` marker (via markdown/paste/edit, not the in-app editor) is reinterpreted as a mention/date on round-trip — low-probability, documented in `MentionDateMarkers.swift`. Spec/plan: `docs/superpowers/{specs/2026-06-18-richtext-composer-bridge-gaps-design.md,plans/2026-06-18-richtext-composer-bridge-gaps.md}`.

**Composer code blocks — first-class `Block.code` (added 2026-06-19, `feature/richtext-code-block`).** Code blocks now **losslessly round-trip and are creatable** in the native composer (previously they degraded to a `.body` paragraph). The editor gained a first-class multi-line `Block.code` — a Core `CodeBlock` model + a `CodeBlockBox` monospace rendering box — that **reuses the newline-agnostic position model + composer flat-mapping**, so a multi-line code block needs no new position machinery (it slots in like a wrap-heavy paragraph; `Block.code` maps to a `.paragraph` `DocNode` with a `TextNodeRef.code` leaf). The bridge round-trip lives in `ComposerDocumentBridge` (a two-pass `document(from:)` that carves contiguous `.block`/.code regions into one `CodeBlock`, preserving interior `"\n"` + language; symmetric emit) and `EntityMessageBuilder` (send → `.Pre(language)`), both via shared `TextFormat` helpers `CodeBlockMarkers.swift` (`chatInputCodeBlockAttribute` / `codeBlockRanges`, unit-tested in `//submodules/TextFormat:TextFormatTests`). **Format▸Code is wired** (`performFormatAction(.code)` → `RichTextEditorView.makeCodeBlock()` create/toggle-off). Editing UX mirrors the quote affordances: Enter inserts an interior newline (and exits to a body paragraph on an empty trailing line), backspace in an empty code block un-codes it to body, a tap below a trailing code block appends a paragraph; cross-block edits truncate-and-keep the code block (a fully-covered selection drops it, media-parity). A **free bonus**: multi-line quotes now emit one contiguous blockquote on send (the same grouping that contiguates code). Code is **entity-expressible** (`.Pre`), so `documentNeedsRichLayout` returns false for it (normal text+entities path); the rich `InstantPageBuilder` path emits `.preformatted`. **Build + all suites green** (Core 102, UIKit 714, TextFormatTests, full app build); the **logged-in-sim runtime pass is pending** (non-gating, per spec). Deferred (spec non-goals): a language picker (creation sets `language = nil`; an incoming language round-trips), code blocks inside table cells, and inline formatting inside a code block. Spec/plan: `docs/superpowers/{specs/2026-06-19-richtext-code-block-design.md,plans/2026-06-19-richtext-code-block.md}`.

**ChatInputContent model seam — replacing `NSAttributedString` as the composer currency (in progress, `feature/chat-input-content-model`, 2026-06-19).** A phased migration introduces a purpose-built, TelegramCore-native value model `ChatInputContent` (`submodules/TelegramCore/Sources/ChatInputContent/ChatInputContentModel.swift`: block/run tree mirroring the editor `Document` — body/quote/**collapsedQuote**/code blocks + inline bold/italic/mono/strike/underline/spoiler + entity mention/url/date/`customEmoji(fileId:file:enableAnimation:)`; public-opaque `ChatInputSelection`) with a bidirectional, **display-neutral** TextFormat utility (`Sources/ChatInputContentConversion.swift`: `chatInputContent(from:)` / `attributedString(from:)`, round-trip-identity tested in `//submodules/TextFormat:TextFormatTests`). **Landed:** Plan 1a (foundation), 1b-i (native node routes content set through the model), and **1b-ii minimal seam** — the LEGACY (production) node `ChatRichTextInputNodeImpl` now owns its set-decoration: `applyRenderingConfig(...)` (host theme/emoji inputs) + `setInputContent` (model → `textAttributedStringForStateText`, byte-identical to the old host path) + `currentInputContent` (`stateAttributedStringForText` → model). The chat layer routes the **four content-SET sites** (the two `updateInputTextState`, `insertText`, spoiler-reveal re-decorate) through `applyRenderingConfig`+`setInputContent`; each keeps its existing refresh/`didUpdateText` ordering. **The READ-BACK (`inputTextState`) deliberately stays on `stateAttributedStringForText`, NOT the model:** `ChatTextInputState.==` uses `inputText.isEqual(to:)` and `ChatTextInputTextCustomEmojiAttribute.isEqual` is **reference** equality, which the model's freshly-minted objects would break — the GET path moves to the model only once `ChatTextInputState` adopts value equality (Piece 3). Behavior-identical by construction (the model round-trip is identity over the carried attribute set, incl. multi-line quotes emitted as **one contiguous `.block`** — a review-caught fragmentation fix); model fidelity matches the canonical `ChatTextInputStateText` (drops `interactivelySelectedFromPackId`/`custom`, like a draft save/restore). Build + TextFormatTests green, and the **legacy-composer runtime pass is verified** (2026-06-19: formatting / spoiler reveal / custom emoji / single- + multi-line quote / collapsed quote / code / mention, plus send and draft restore). **Piece 2 (decoration relocation, Tasks 1–4) — DONE.** The legacy node now **owns its decoration**: SET-path in-place fix-up folded into `setInputContent` (Task 1, threads `primaryTextColor`); per-keystroke decoration collapsed to one node method `decorateAfterTextChange` (Task 2, a default protocol-extension forwarding the 3 steps — live values, no staleness, chosen over a pure observer for lower risk); the **spoiler-reveal flow** (`updateSpoilersRevealed` default-impl owns detection + 1.5s hide delay + dust cross-fade; the node owns `spoilersRevealed`) (Task 3); and **paste / plain-text fragment decoration** (`decorateReplacementFragment`; the `text` setter routes through the model) (Task 4). The panel no longer calls `refreshTextInputAttributes`/`textAttributedStringForStateText`/`prepareForSpoilerReveal`/`setSpoilersRevealed` on the legacy node. **Accepted residuals (deliberately left — the model migration subsumes them, so hand-relocating is wasted effort):** `refreshTextInputTypingAttributes` (load + selection), `updateSpoiler`→`updateRichRendering` (SET/load redundant, theme), and the theme-change re-color block (**now fixed 2026-06-19** — see below). Build-green throughout; Tasks 1–3 runtime-verified, Task 4 build-verified (paste/clear pending the next pass). **Two pre-existing bugs surfaced during the runtime pass (NOT Piece-2 regressions — both subsumed by the migration):** typing inside a spoiler splits it (no `refreshSpoilers` heal + `typingAttributes` drops the spoiler attribute → fixed by the native editor's run-attribute inheritance, NOT the model alone since spoiler stays an inline run attribute); multi-line quote splits on draft save/restore (flat object-identity fragmentation via `enumerateAttribute` over uncovered newlines → fixed by structural blocks + structural persistence, Piece 6 — the SET conversion already re-contiguates, guarded by `test_chatCurrencyRoundTrip_multiLineQuote_perLineObjects_recontiguates`). Deferred: Pieces 4–6 (~256-consumer migration → flip & remove NSAttributedString → persist+cloud `DraftMessage.richMessage`). **Piece 3 (state adopts the model) — DONE.** `ChatTextInputState.==` is now value-based (`submodules/AccountContext/Sources/ChatController.swift`): fast-path on `selectionRange` + `inputText.isEqual(to:)`, else compare `chatInputContent(from:)` value models — **strictly coarser-or-equal** to the old reference equality (can only remove churn, never add it), absorbing custom-emoji object identity / `interactivelySelectedFromPackId` / `file`, matching the persisted `ChatTextInputStateText` fidelity. That unlocked the **GET read-back** (`ChatTextInputPanelNode.inputTextState`), which now routes through `attributedString(from: currentInputContent().content)` (the Piece-3 pin is gone). **Design A** (scope value equality to the state type) was chosen over **Design D** (global value-based `ChatTextInputTextCustomEmojiAttribute.isEqual`) because that attribute lives in ~90 files **incl. message rendering** — A confines the change; investigation found **zero** `===`/`ObjectIdentifier` reliance on the attribute (the one direct compare in `ComposerDocumentBridge` is a workaround that *wants* value semantics), and `AccountContext` already imports `TextFormat`+`TelegramCore` with no cycle. Build + TextFormatTests green (added `test_modelEquality_sameEmojiDistinctObjects_areEqual` + `_differentFileId_areNotEqual`); **logged-in-sim runtime pass verified** (2026-06-19: formatting / custom emoji / idle-no-churn / draft save+restore / send / recents bump / undo-redo). **Piece 4 (model read surface) — foundation + batch 1.** Added `ChatInputContent.plainText`/`.length` (TelegramCore, pure Foundation, parity-tested) + a `ChatTextInputState.content` accessor; migrated 4 cold standalone `.inputText.string` reads to `content.plainText`. The genuine `ChatTextInputState.inputText` read surface is **71 sites / 23 files** (the earlier "~256" conflated `TextFieldComponent.InputState`, a separate type — excluded); further read migration is **optional** under Option A (below). **Piece 5 (structural storage + recursive selection) — DONE (stages 1–2), build+tests green, **runtime-verified** (incl. draft caret/selection round-trip).** Chose **Option A** (keep `inputText` as a derived view, don't migrate all 71 readers). Stage 1: replaced the opaque `ChatInputSelection` with a **recursive tree-path** selection — `ChatInputPathStep {blockIndex, slot}` / `ChatInputPosition {path, offset}` / `ChatInputSelection {start, end}` (slot/path chosen for nested quotes + planned collapsible-detail blocks) + a content-aware flat↔structural bridge (`position(forFlatOffset:)`/`flatOffset(for:)`/`nsRange(in:)`/`init(nsRange:in:)`, round-trip-identity tested), with the node boundary + the panel's 6 SET sites converting through it (flat `NSRange` stays encapsulated UITextView plumbing). Stage 2: `ChatTextInputState` now **stores** `content` + structural `selection`; `inputText`/`selectionRange` are **derived** transitional compat; `==` is `content == && selection ==`; **Codable persisted form is UNCHANGED** (`ChatTextInputStateText` + flat ints, via the derived views) so drafts round-trip; the one in-place `inputText =` assignment (`ChatInterfaceInputContexts`) was rebuilt. **Perf:** derived `inputText` allocates per read, so the emptiness checks were migrated to a cheap non-allocating `ChatInputContent.isEmpty` / `ChatTextInputState.isEmpty` (replacing `inputText.string.isEmpty` / `.length == 0` at ~16 sites; one pre-existing naming-bug pair in `ChatControllerNode` left as-is). Remaining derived-`inputText` reads (`.string` for query, `.enumerateAttributes`) still allocate but are lower-frequency — watch the runtime pass. **Runtime fix (theme re-color, the Piece-2 residual above):** a pre-existing bug surfaced during the Piece 5 pass — switching theme (or restoring a draft, which applies the theme color) re-colored the whole composer via a naive full-range `foregroundColor = inputTextColor` rewrite (panel), revealing custom-emoji / unrevealed-spoiler base glyphs (which must stay `.clear` under their overlays) and dropping the accent on mentions/urls/dates, until the next keystroke healed them. **Fixed** by routing the theme-change re-color through the node's `decorateAfterTextChange` (correct per-attribute colors + overlay rebuild), subsuming the residual. **Runtime fix (draft caret/selection):** another pre-existing bug — draft restore never preserved the caret. The persisted selection (`SynchronizeableChatInputState.textSelection`) was both (a) silently dropped by the SyncCore Codable (`encode` omitted it, `init(from:)` hard-coded `nil`) and (b) ignored by the restore (`ChatInterfaceState.withUpdatedSynchronizeableInputState` rebuilt via `ChatTextInputState(inputText:)` = caret-at-end), and `ChatInterfaceState.parse` always lets the synchronizeable flat form win over the decoded `opaqueData`. Fixed both: persist `ts0/ts1` + read `textSelection` on restore (`a0aeccf602`, `9cd036f182`; verified). **This mapped the draft-persistence architecture for Piece 6** (two layers — `opaqueData` rich Codable vs the flat `synchronizeableInputState`, the latter authoritative via `parse`; the `textSelection` round-trip is the precedent for threading the structural model through) — see the Piece 6 handoff. Consumer-API direction: selection mutations should become **semantic methods** (insert-and-select, replace-selection), not raw index arithmetic. Spec/plan: `docs/superpowers/{specs/2026-06-19-chat-input-content-model-seam-design.md,specs/2026-06-19-chat-input-structural-selection-design.md,plans/2026-06-19-chat-input-content-model-*.md}` + handoffs `handoffs/2026-06-19-chat-input-model-{plan-1b-ii,piece-2,piece-3,piece-4}-handoff.md`. **Piece 6 (structural draft persistence) — DONE (build-green; logged-in-sim runtime pass pending).** Refined from the handoff's `richContent: Data?` idea to a cleaner shape (user-directed): the local layer holds **only `ChatInputContent`** (now `Codable` — `ChatTextInputState` persists it under `"cm"`, back-compat-decoding the legacy `"at"` `ChatTextInputStateText`; **⚠️ persisted via `AdaptedPostbox{En,De}coder`, which supports NO `singleValueContainer` and NO bare `Int`** — so the model's enum discriminators are encoded as explicit `Int32` rawValues, never `encode(SomeEnum.case)` directly, and all numbers are explicit `Int32`/`Int64`; a JSON round-trip test masks this, so the model Codable is tested via `AdaptedPostboxEncoder`), and `SynchronizeableChatInputState` carries a `.textEntities(text,entities) | .instantPage(InstantPage)` **`Content` enum** (derived `.text`/`.entities` accessors keep the chat-list draft preview + cloud-sync read sites unchanged; legacy `t`/`e` Codable preserved). The save/restore (`ChatInterfaceState.synchronizeableInputState` / `withUpdatedSynchronizeableInputState`) route through the **model conversion** (`.textEntities`/`nil` paths byte-identical to before; the save deliberately keeps `expandedInputStateAttributedString` so a collapsed quote's folded content survives into the entities). **Re: the multi-line-quote draft split** — a final holistic review found the *composer* split was already resolved at Piece 5 (model-as-storage + the contiguating `chatInputContent(from:)`); Piece 6's **unique** contribution is the structural **`"cm"` local Codable**, load-bearing for the **edit-message** draft (`editMessage.inputState`): `ChatInterfaceState.parse` always overrides `composeInputState` from the flat synchronizeable form but does **not** override `editMessage.inputState`, so `"cm"` is redundant-but-harmless for the composer yet preserves structure the fragmenting `"at"` round-trip would lose for edit-message. (The logged-in-sim runtime pass should specifically exercise an **edit-message** multi-line/collapsed-quote draft round-trip — the one path Piece 6 uniquely fixes.) The `.instantPage` branch is **forward-looking** — production always selects `.textEntities` because **everything `ChatInputContent` can represent today is entity-expressible** (`ChatInputContent.isEntityExpressible == true`, incl. collapsed quotes via `.BlockQuote(isCollapsed:)`); the InstantPage path is proven by round-trip tests until a non-entity block (tables/lists) lands. **InstantPage carrier (built full):** a new lossless `ChatInputContent ↔ InstantPage` pair in TelegramCore (`Sources/ChatInputContent/ChatInputContentInstantPage.swift`, `instantPage(from:)` / `chatInputContent(fromInstantPage:)`) using the full `RichText` cases, + `collapsed: Bool?` on `InstantPageBlock.blockQuote` (local Postbox-Codable key `"qcol"` only — **not** on the MTProto wire / FlatBuffers; 23-site arity sweep). **Cloud wire reuses the existing message machinery:** `messages.saveDraft`'s `richMessage:` (flag `1<<9`, caller-set) sends `RichTextMessageAttribute(instantPage:).apiInputRichMessage()` for `.instantPage`; the incoming `updateDraftMessage` parse (`AccountStateManagementUtils`) reads `draftMessageData.richMessage` (was ignored) → `RichTextMessageAttribute(apiRichMessage:).instantPage`. **Two documented fidelity caveats (both match the `.textEntities` path / required for cloud):** custom-emoji `enableAnimation` has no `RichText` carrier so it canonicalizes to `true` on the reverse (re-derived at decoration, like the entity path; pinned by `test_customEmoji_enableAnimationFalse`); the three model quote states map onto one `collapsed: Bool?` so `.quote(isCollapsed:false)`/`.collapsedQuote` are strict round-trip identity while the rare `.quote(isCollapsed:true)` normalizes to `.collapsedQuote` (same sent message), and `nil`/`false` → visible quote (the MTProto wire has no collapsed field, so this is required — else every cloud-synced quote would fold). Full app build + `TextFormatTests` green (added `ChatInputContentInstantPageTests` round-trip matrix + `SynchronizeableChatInputStateTests` Codable round-trip + `ChatInputContent` Codable). Spec/plan: `docs/superpowers/{specs/2026-06-19-chat-input-structural-draft-persistence-design.md,plans/2026-06-19-chat-input-structural-draft-persistence.md}`. **Remaining (deferred, spec non-goals):** cloud `DraftMessage.richMessage` cross-device collapse fidelity (Api has no flag), a code-block language picker, and the eventual non-entity block types that will actually exercise the `.instantPage` branch. **Full Document parity — structural blocks survive drafts (heading/list/table) — DONE (build-green; runtime pass pending), `feature/chat-input-content-model` 2026-06-20.** The first piece that actually exercises Piece 6's `.instantPage` path. `ChatInputContent` grew to **mirror the editor `RichTextEditorCore.Block` 1:1**: `.media(ChatInputMedia{media:Media,kind,size,displayWidth,alignment,caption})` + `.table(ChatInputTable{columns,rows,inline-only cells})` blocks, heading1-3 styles + `list:ChatInputListMembership?` on paragraphs (Postbox-safe `Codable` — same `singleValueContainer`/`Int` rules; **the polymorphic `Media` persists via a concrete-type discriminator + `TelegramMediaImage/File(decoder:)`, NOT `decodeRootObjectWithHash` — that needs the app-startup `declareEncodable` registry which is empty in unit tests**). `isEntityExpressible` returns false for media/table/heading/list → they route to `.instantPage`. The `ChatInputContent↔InstantPage` converter gained heading/list/table/media arms reusing the existing cloud `apiInputBlock` wire (canonicalizations: list level→0, table column-width/valign/colspan/cell-background defaults + `justified→left`, **heading+list → heading** — the list-coalescing MUST guard on `.body` or a non-body list paragraph infinite-loops the main thread on the synchronous draft-save path, a review-caught hang; emoji `altText`/`instanceID` dropped). A new **direct `Document ↔ ChatInputContent` bridge** (`submodules/TelegramUI/Components/Chat/ChatRichTextEditorComposer/Sources/DocumentChatInputContentBridge.swift`) is the load-bearing change — the native node's `currentInputContent`/`setInputContent` use it, bypassing the `NSAttributedString` hop where structural blocks died; inline runs reuse `classifyChatLink`/`MentionDateMarkers` + emoji/media resolver closures, dropping non-chat attrs (font/color/baseline). **Completed the "no NSAttributedString storage" boundary:** `ChatTextInputPanelNode.inputTextState` (GET) + the 5 state-driven SET sites now pass `ChatTextInputState.content` **directly** (not `chatInputContent(from: state.inputText)`, which flattened structural) — equivalent for the legacy node (flat, round-trip identity), lossless for native. The **legacy `UITextView` composer** is a render-only lossy filter (`attributedString(from:)` drops media/table, renders heading/list as plain text). **MEDIA was the one DEFERRAL** (heading/list/table are full parity) — **now landed (NEXT STEP A, `feature/chat-input-content-model` 2026-06-20, build-green + runtime-verified 2026-06-20):** attached image/video **renders inline** in the native composer and **survives** the expand↔collapse round-trip + drafts, carried losslessly by `ChatInputContent`. Render: the native node forwards a `mediaItemViewFactory` closure to the editor's `registerMediaViewProvider` (resolving its private `mediaByID`); the concrete view is the shared **`RichTextEditorMediaView.MediaItemNodeView`** (extracted from the article editor — both now use the one view). **Cycle constraint (load-bearing):** `InstantPageUI` already transitively deps `ChatTextInputPanelNode`, so the panel/node **cannot** dep `InstantPageUI`/`RichTextEditorMediaView` — `TelegramUI` (top, above the cycle) builds the view and injects the factory into the panel at its 2 construction sites; the panel/node carry only the closure *type* (media crosses the protocol as `EngineMedia`, keeping `ChatInputTextNode` Postbox-free). Handoff: `richTextDocument`/`setRichTextDocument` → `expandedEditorSeed()`/`setFromExpandedEditor(document:media:)`, routing inbound through `setInputContent` so the model owns the media (and `mediaByID` repopulates via the existing `registerMedia`); `RichTextAttachmentScreen` gained `initialMedia` for the outbound seed. **Deferred (documented):** sending a media-bearing composer message as a cloud rich message — cloud-media upload is hard-coded off project-wide (`apiInputRichMessage()` → `photos:nil`), so a plain send drops the medium for now. Spec/plan: `docs/superpowers/{specs/2026-06-20-chat-composer-inline-media-design.md,plans/2026-06-20-chat-composer-inline-media.md}`. Full app build (×N) + `TextFormatTests` green (added structural-block AdaptedPostbox Codable + `ChatInputContent↔InstantPage` arm round-trips + a heading+list anti-hang regression + a legacy-filter test). **Runtime pass MUST also verify the legacy production composer is not regressed** by the panel content-direct change. Spec/plan: `docs/superpowers/{specs/2026-06-20-chat-input-full-document-parity-design.md,plans/2026-06-20-chat-input-full-document-parity.md}`. **Native caret-drift fix (NEXT STEP B of the media-followup handoff) — DONE (`d3c94d04b4`, build+TextFormatTests green; runtime pass owed).** A `.media`/`.table` block was drifting the native composer caret by the count of preceding non-text blocks. Root cause was a **model-internal invariant violation**, NOT the editor/tree-path the review guessed: `ChatInputContent.plainText` claims to equal `attributedString(from:).string`, but `attributedString(from:)` (and the editor's flat `composerSelectedRange`/`composerParagraphs()`) drop media/table to **0 chars + no separator**, while `plainText`/`blockFlatLength` gave them a 1-char `" "` placeholder + bracketing `"\n"`s (mis-copied from `.collapsedQuote`, which legitimately IS one placeholder char in the projection). So the node decoded an editor flat offset (media=0) against the model (media=1) → the caret slid into the block; it also fixed a latent `ChatTextInputState` split (`inputText` media-0 vs `selectionRange` media-1). Fix is **purely in the model** (editor/node unchanged): `.media`/`.table` are now off the flat axis across `plainText`/`blockFlatLength`/`flatOffset`/`position(forFlatOffset:)` via `blockIsFlatParticipating`, restoring the invariant. Byte-identical for any media/table-free content (so legacy/production is untouched); `.collapsedQuote` stays 1; `isEmpty` stays content-aware. **NEXT STEP A (editor↔node `mediaID→Media` store plumbing so editor-created media round-trips and pushed media renders) + the runtime pass remain** before the branch is done. Handoff: `docs/superpowers/handoffs/2026-06-20-chat-input-parity-media-followup-handoff.md`. **Expand/collapse routed through `ChatInterfaceState` (in & out) — DONE (`025dca7434`, build-green; runtime pass owed).** `ChatControllerNode.openExpandedInput` no longer pokes the composer node — the chat input STATE is the single source of truth: OUT converts the live `inputTextState.content` → `(Document, media)` to seed `RichTextAttachmentScreen`; IN converts the editor's `(document, media)` → `ChatTextInputState` applied via `updateChatPresentationInterfaceState` + `withUpdatedEffectiveInputState` (the `openAICompose` precedent, so undo/drafts/send/observers see one value). New thin converters `ComposerExpandedEditorBridge.swift` (`documentAndMedia(fromChatInputContent:)` / `chatInputContent(fromDocument:media:)` + shared `composerMediaKey`) wrap the existing `Document↔ChatInputContent` bridge; the node's `expandedEditorSeed()`/`setFromExpandedEditor` were deleted (protocol + both conformers). Inbound custom emoji resolve `file: nil` (chat decoration re-derives; behavior-equivalent to the deleted node path). TelegramUI deps `ChatRichTextEditorComposer` (cycle-free). Handoff: `docs/superpowers/handoffs/2026-06-20-chat-composer-expand-via-interface-state-handoff.md`.

## Embedded watch app (`Telegram/WatchApp`)

A standalone watchOS Telegram client (developed in the separate `~/build/tgwatch` repo) is vendored into this repo at `Telegram/WatchApp/` and can be embedded into the **device** IPA under `Telegram.app/Watch/`. It is built by `xcodebuild` (not Bazel) and codesigned by the Bazel build.

**Build it:** add `--embedWatchApp` to a Make.py **device** build (`--configuration=debug_arm64` or `release_arm64`) together with `--watchApiId`, `--watchApiHash`, `--watchSigningIdentity`, `--watchProvisioningProfile`. Off by default (it adds a ~4-min xcodebuild step); simulator builds never embed, and the default `debug_sim_arm64` build is unaffected.

**`Telegram/WatchApp/` is a synced snapshot — do not hand-edit it.** The source of truth and dev tooling live in the `tgwatch` repo. To change the watch app, edit it there, then re-sync with `tgwatch/tools/export-sources.sh /abs/path/to/telegram-ios/Telegram/WatchApp` and commit the result. The committed `tgwatch.xcodeproj` is generated (kept via a `!tgwatch.xcodeproj` negation in `Telegram/WatchApp/.gitignore`, since the root `.gitignore` ignores `*.xcodeproj`); `.build`/`.swiftpm`/`xcuserdata` are excluded.

**How it's wired:** `//Telegram:TelegramWatchApp` (rule in `Telegram/prebuilt_watchos.bzl`) runs in **two actions**: `PrebuiltWatchosCompile` (`Telegram/prebuilt_watchos_compile.sh`) runs xcodebuild on the snapshot in a writable temp copy with PLACEHOLDER version/api values (the bundle ids are baked from the snapshot's pbxproj/Info.plist — `ph.telegra.Telegraph.watchkitapp` / `ph.telegra.Telegraph`), emitting an unsigned `.app`; `PrebuiltWatchosPatchSign` (`Telegram/prebuilt_watchos_patch.sh`) then rewrites **six** per-build Info.plist keys (`CFBundleShortVersionString`, `CFBundleVersion`, `TG_API_ID`, `TG_API_HASH`, `CFBundleIdentifier`, `WKCompanionAppBundleIdentifier`) and codesigns the `.app` + nested `TDLibFramework.framework` (identity + the watchkitapp profile from `--define`s). The result feeds the `Telegram` `ios_application`'s `watch_application` slot (gated by the `//Telegram:embedWatchApp` flag). The rule takes `bundle_id` (set to `"{telegram_bundle_id}.watchkitapp"` in `Telegram/BUILD`) and derives the host bundle id by stripping the `.watchkitapp` suffix; both are passed to the patch worker as args (not action inputs), so the patch action re-runs when the host bundle id changes but the (expensive) compile stays cached. **The compile action's only inputs are the snapshot (+ its worker)** — so changing the version, build number, api id/hash, host bundle id, or signing identity re-runs only the cheap patch+sign action, not xcodebuild; xcodebuild re-runs only when the snapshot changes. This is correct because none of those values reach the compiled binary: each lands only in the Info.plist (via `$(...)` substitution and a runtime `Bundle.main.object(forInfoDictionaryKey:)` lookup in `Secrets.swift`, except for the bundle-id keys which only Info.plist consumers read).

**Non-obvious invariants** (also in the `.bzl` comments): `AppleBundleInfo`'s public init is banned — use the internal `new_applebundleinfo`; `watch_application` requires BOTH `AppleBundleInfo` (with a non-None `infoplist` File) AND `WatchosApplicationBundleInfo`; the embedded watch app's `CFBundleShortVersionString`/`CFBundleVersion` must exactly equal the host's (sourced from `versions.json['app']` + `--define=buildNumber`); the host does NOT re-sign the embedded watch app, so the worker must sign it; the watch bundle id `ph.telegra.Telegraph.watchkitapp` must track the host `telegram_bundle_id`.

**Status:** verified with **development** signing on `debug_arm64` only. Open follow-ups before App Store shipping: secure timestamp (drop `codesign --timestamp=none`), distribution profile (`get-task-allow=false`), `release_arm64` + `altool --validate-app`, and committing a `Package.resolved` for hermetic remote-SwiftPM resolution.

## View frame ownership

A view does not control its own `frame`. The parent (or a layout system) sets the frame; the view positions its own subviews against `self.bounds` in response.

This matters in two places specifically:

- **Reusable components (`UIView`/`ASDisplayNode` subclasses).** Public methods like `update(...)` / `apply(...)` rebuild internal state, mutate child frames, and read `self.bounds` to lay them out — but they do not write `self.frame`. The caller has already chosen the frame; mutating it from inside the component overrides that choice and fights the parent's next layout pass.
- **`asyncLayout`-style content nodes.** The measure pass runs off-main and returns a size; the apply step runs on main and the chat layout system positions the node. A child view that writes `self.frame` from `update()` corrupts the size the parent just measured.

Rare exceptions: top-level view-controller views integrating with the system's first-responder/inset model. If you find yourself wanting `self.frame = …` from inside a child view, refactor so the parent positions it instead.

## ChatHistoryListNode composition

`ChatHistoryListNodeImpl` (`submodules/TelegramUI/Sources/ChatHistoryListNode.swift`) **composes** rather than inherits `ListViewImpl` (`submodules/Display/Source/ListView.swift`): it is an `ASDisplayNode` wrapper holding `private let listView: ListViewImpl` and exposes a deliberately narrowed surface (the `ChatHistoryListNode` protocol in `AccountContext` + curated concrete forwarders) instead of the full `ListView` API. `ListViewImpl` gained a `getCustomItemDeleteAnimationDuration` closure hook so the one former `override` works via composition.

These invariants are **compiler-invisible** — getting them wrong silently breaks the app's primary scroll surface:

- **The π rotation stays on the wrapper** (chat is bottom-up). The wrapper keeps `transform = π` + a `rotated` flag; the child `listView` gets only `rotated = true` (identity transform). So `historyNode.view`/`.layer` remain the rotated surface, and rotation-coupled code — hitTest coordinate conversions, the blur `drawHierarchy` flip, the dust/delete layer, `.layer` animations, and the overscroll-overlay + snapshot-slide reparenting — **stays on `self` (the wrapper)** unchanged.
- **Only genuine scroll-surface concerns route to the child:** gesture recognizers (selection pan; external taps via `addContentGestureRecognizer`) attach to `self.listView.view` to share the scroll pan's simultaneity environment, and scroller access goes to `self.listView.scroller`.
- **`let _ = self.view` in `init` is load-bearing.** The old inherited node was view-loaded eagerly (so `self.isNodeLoaded` was always true); `enqueueHistoryViewTransition` gates the history dequeue on it. The wrapper must force-load its view in init or off-screen nodes (created during thread switches) never become ready and `reloadChatLocation`'s completion never fires.
- **Item nodes are one level deeper.** Any `.supernode` chain / hierarchy-depth assumption passing through the history node gained one level (item → child `listView` → wrapper). E.g. `ChatMessageTransitionNode` converts item rects up `supernode?.supernode?.supernode?.view` (was 2 hops) so the wrapper's rotation is applied as an intermediate transform; a missing hop reflects effect-burst overlays ~180°.
- Child geometry is driven inside `updateLayout` via `transition.updateFrame(node: self.listView, …)` — the project never relies on ASDisplayNode's automatic `layout()`.

The public surface is being narrowed incrementally (e.g. `scroller` → `bounces`/`contentHeight`; the `trackingOffset`/`beganTrackingAtTopOrigin` pair → `didInteractivelyDragFromTopOrigin`). Prefer intent-named accessors over re-exposing raw `ListView` state.

## InstantPage V2 & rich-text messages

Typed markdown with structure the regular message-entity set can't represent (headings, lists, tables, formulas, nested blockquotes) is sent as a **rich message** — a `RichTextMessageAttribute` carrying an `InstantPage`, drawn by `ChatMessageRichDataBubbleContentNode` via the **InstantPage V2** renderer (with AI-streaming progressive reveal, inline custom emoji, and entity cases). The detailed architecture and non-obvious invariants — streaming reveal, V2 table/text-box layout, custom-emoji & entity round-trips, task-list checkboxes, nested blockquotes, thinking blocks, the markdown send / edit / copy / paste paths, and surfacing rich-message media through the shared-media/gallery/preview pipelines via `Message.effectiveMedia` — live in [`docs/instantpage-richtext.md`](docs/instantpage-richtext.md).

## Postbox → TelegramEngine refactor (in progress)

A gradual migration is underway to eliminate direct `import Postbox` from consumer submodules in favor of `TelegramEngine`.

**Historical record:** Wave-by-wave outcomes, the running tally of Postbox-free modules, the full wave-selection guidance, and the `TelegramEngine.Resources` facade inventory (also authoritatively defined in `submodules/TelegramCore/Sources/TelegramEngine/Resources/TelegramEngineResources.swift`) live in [`docs/superpowers/postbox-refactor-log.md`](docs/superpowers/postbox-refactor-log.md). Read that file when you need wave-specific context, a full worked example of a pattern, or the history of a particular module's migration.

See the log for per-wave detail; the current wave count and the list of still-open migration opportunities live in the `project_postbox_refactor_next_wave.md` memory file.

### Rules that apply to every wave

1. `TelegramCore` does **not** `@_exported import Postbox`. Once a consumer drops `import Postbox`, every remaining Postbox-type reference must use an engine-typealiased equivalent.
2. **Never typealias `Postbox`, `Account`, or `MediaBox`.** These umbrella types rename without encapsulating. Narrow utility typealiases (`MemoryBuffer`, `PostboxDecoder`, `PostboxEncoder`, `AdaptedPostboxDecoder`, `MediaResource`, …) remain allowed and expected.
3. No new engine wrapper **structs** unless the wave's spec explicitly allows — only typealiases and thin forwarding methods.
4. **Discovery first:** before adding any new engine wrapper/typealias, grep `submodules/TelegramCore/Sources/TelegramEngine/` for existing equivalents. Record the search result in the commit message.
5. **Abandonment protocol:** if a module can only be refactored by violating rule 2 or by editing a module outside the current wave's list, mark the task Abandoned with a recorded reason. Do NOT substitute a new module mid-wave.
6. Full project build per module. No unit tests exist in this project.
7. **TelegramCore never imports UIKit/Display.** `TelegramCore` is shared with the Telegram-Mac codebase; its Bazel `deps` and source files must not reference UIKit, Display, or any Apple-UI framework. UIKit-needing helpers (image scaling, rendering, etc.) stay in consumer-side submodules.
8. **Never substitute Postbox protocols (`Media`, `Peer`, `Message`) with `Any` / `AnyObject`** in code that previously used them. Type erasure throws away the domain semantics that the next reader expects. Use the matching engine wrapper (`EngineMedia`, `EnginePeer`, `EngineMessage`) — extending it as needed (e.g. add a missing case-init or convenience). If neither typealias nor wrapper covers the use site, restore the original Postbox import + type for now and flag the case for a future facade. Existing `Any`/`AnyObject` parameters predating the refactor are not in scope for this rule.

### Engine typealias cheat sheet (existing aliases)

```
PeerId              → EnginePeer.Id
MessageId           → EngineMessage.Id
MessageIndex        → EngineMessage.Index
MessageTags         → EngineMessage.Tags
MessageAttribute    → EngineMessage.Attribute
MessageFlags        → EngineMessage.Flags
MessageForwardInfo  → EngineMessage.ForwardInfo
MediaId             → EngineMedia.Id
PreferencesEntry    → EnginePreferencesEntry
TempBox             → EngineTempBox
PinnedItemId        → EngineChatList.PinnedItem.Id
MemoryBuffer        → EngineMemoryBuffer           (added 2026-04)
PostboxDecoder      → EnginePostboxDecoder         (added 2026-04)
PostboxEncoder      → EnginePostboxEncoder         (added 2026-04)
AdaptedPostboxDecoder → EngineAdaptedPostboxDecoder (added 2026-04)
ItemCollectionId    → EngineItemCollectionId       (added 2026-04-20)
FetchResourceSourceType → EngineFetchResourceSourceType (added 2026-04-20)
FetchResourceError  → EngineFetchResourceError     (added 2026-04-20)
StoryId             → EngineStoryId                (added 2026-05-02)
ChatListIndex       → EngineChatListIndex          (added 2026-05-03)
TempBoxFile         → EngineTempBoxFile            (added 2026-05-03)
ItemCollectionItemIndex → EngineItemCollectionItemIndex (added 2026-05-03)
ItemCollectionViewEntryIndex → EngineItemCollectionViewEntryIndex (added 2026-05-03)
ValueBoxEncryptionParameters → EngineValueBoxEncryptionParameters (added 2026-05-03)
MessageAndThreadId  → EngineMessageAndThreadId      (added 2026-05-03)
PeerStoryStats      → EnginePeerStoryStats          (added 2026-05-03)
MessageHistoryAnchorIndex → EngineMessageHistoryAnchorIndex (added 2026-05-03)
ChatListTotalUnreadStateCategory → EngineChatListTotalUnreadStateCategory (added 2026-05-03)
ChatListTotalUnreadStateStats → EngineChatListTotalUnreadStateStats (added 2026-05-03)
PeerSummaryCounterTags → EnginePeerSummaryCounterTags (added 2026-05-03)
ChatListTotalUnreadState → EngineChatListTotalUnreadState (added 2026-05-04)
ItemCacheEntryId    → EngineItemCacheEntryId        (added 2026-05-04)
HashFunctions       → EngineHashFunctions           (added 2026-05-04 wave 251)
CachedMediaResourceRepresentationResult → EngineCachedMediaResourceRepresentationResult (added 2026-05-04 wave 265)
MediaResourceDataFetchResult → EngineMediaResourceDataFetchResult (added 2026-05-04 wave 266)
MediaResourceDataFetchError → EngineMediaResourceDataFetchError (added 2026-05-04 wave 266)
MediaResourceStatus → EngineMediaResourceStatus     (added 2026-05-04 wave 272)
```

**Free-function thin forwarders in TelegramCore** (rule 3 allows):
- `engineFileSize(_ path:, useTotalFileAllocatedSize: Bool = false)` — forwards to Postbox's `fileSize(...)` (added 2026-05-04 wave 268)

**TelegramEngineUnauthorized.resources facade**: `UnauthorizedResources.storeResourceData(id: EngineMediaResource.Id, data:, synchronous:)` — bridges to `account.postbox.mediaBox.storeResourceData` (added 2026-05-04 wave 271)

For the `MediaResource` Postbox protocol, prefer the TelegramCore subtype `TelegramMediaResource` when the consumer's usage allows (note: `EngineMediaResource` is a wrapper **class**, not a typealias, so it is not interchangeable with the protocol).

### MediaResource → EngineMediaResource consumer migration

`EngineMediaResource` is a `final class` in `TelegramCore` wrapping a `MediaResource` value. Unlike the typealiases above it is **not** interchangeable with the protocol, but it does provide wrap/unwrap helpers:

- `EngineMediaResource(rawResource)` — wrap a raw `MediaResource`.
- `engineResource._asResource()` — unwrap to the raw `MediaResource`.
- `EngineMediaResource.ResourceData(rawResourceData)` — wrap `MediaResourceData`.
- `EngineMediaResource.Id(rawMediaResourceId)` — wrap `MediaResourceId`.

**Pattern for facade functions:** when a `TelegramEngine.<Area>` method leaks raw `MediaResource` in its public signature, **change the facade signature in place** to `EngineMediaResource` (and change any closure parameter types the same way). Bridge inside the facade body by calling the existing `_internal_*` function with `engineResource._asResource()` / wrapping raw inputs from inner closures with `EngineMediaResource(rawResource)`. Update all call sites in the same commit. The `_internal_*` function stays on raw `MediaResource` — it is the Postbox-facing layer.

Do **not** add opt-in `EngineMediaResource` overloads alongside raw-`MediaResource` overloads. Duplicate signatures fragment the public API and leave the leak in place forever.

For consumer modules, prefer `EngineMediaResource` as the type in properties, locals, generic arguments and function parameters when the usage is a pure type reference. Do **not** try to use `EngineMediaResource` where a class must conform to `TelegramMediaResource` (Postbox protocol) or override `isEqual(to: MediaResource)` — those remain `import Postbox`.

## tgcalls Testbench

This repo includes a tgcalls testbench (CLI tool, Go/Pion SFU, Docker build) layered on top of the iOS source. All testbench code, build instructions, and architecture docs live inside the tgcalls submodule:

- `submodules/TgVoipWebrtc/tgcalls/CLAUDE.md` — top-level testbench overview, build/run commands
- `submodules/TgVoipWebrtc/tgcalls/tools/cli/CLAUDE.md` — CLI test tool architecture
- `submodules/TgVoipWebrtc/tgcalls/tools/go_sfu/CLAUDE.md` — Go SFU internals
- `submodules/TgVoipWebrtc/CLAUDE.md` — tgcalls library internals + macOS/Linux build patches

Build the test binary from this directory with:

`./build-input/bazel-8.4.2 build //submodules/TgVoipWebrtc/tgcalls/tools/cli:tgcalls_cli`
